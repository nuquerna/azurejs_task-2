{
  "name": "task_2",
  "version": "1.0.0",
  "scripts": {
    "upload-blob": "az storage blob upload-batch -s './dist' -d '$web' --account-name maebarakeichinuq",
    "start": "terraform apply && npm run upload-blob"
  }
}
